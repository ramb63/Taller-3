#Llenado con datos simulados (Básico)
#Crea un arreglo de 5 edades inicializado en cero y 'cárgalo' con 
# valores = [21, 18, 30, 25, 19].
#Imprime en cada línea: índice → valor.
valores = [21, 18, 30, 25, 19]
indice = 0 
for valor in valores:
    print(indice, '→', valor)
    indice += 1